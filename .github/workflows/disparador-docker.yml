# Build disparador: repositório dedicado https://github.com/victoreder/hublabel-disparador
# Este workflow no monorepo está desativado — use o repo hublabel-disparador.

name: Build and Push Disparador (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use https://github.com/victoreder/hublabel-disparador para build da imagem"
